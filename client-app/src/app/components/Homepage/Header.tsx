import React from "react";
import Carousel from "./Carousel";

const Header = () => {
    return (
        <React.Fragment>
            <Carousel />
        </React.Fragment>
    );
};

export default Header;
